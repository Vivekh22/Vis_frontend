{
  "name": "visprisca-ads-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VispriscaAds — programmatic advertising DSP platform frontend (pure client-side, Web Components, TypeScript).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "happy-dom": "^14.12.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}